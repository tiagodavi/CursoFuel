<!DOCTYPE html>
	<html>
		<meta charset="utf8">
		<title>título</title>
		<div id="conteudo">
			<?php if(isset($title)) 	echo $title; ?>
			<?php if(isset($content)) echo $content; ?>
		</div>
	</html>
